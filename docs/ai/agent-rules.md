# Agent Rules

These rules govern how AI agents should interact with this documentation repository.

## Critical Guidelines
*   **Never delete or overwrite**: `/eos/user/l/lichengz/WEB-PORTAL/external/`
*   **Safe publishing**: Always use `scripts/publish.sh` to publish changes.
*   **Data limits**: Never check large datasets, ROOT files, plots, or HTML logs into Git. Use the external upload scripts.
*   **Credentials**: Never commit API keys, secrets, or internal paths.
